# tp104 检查规则与命中清单工作台

一个本地小工具：把团队约定要检查的写法整理成规则，把需要检查的文件内容收进来，点一下扫一遍，命中的地方会记到具体的文件与行上。规则编码写得不合规矩、同一条规则编码重复使用、文件路径重复收录、内容为空，保存时当场就会指出来。

数据全部存在本机的数据文件里，不连数据库，也不调用任何外部服务。

## 技术栈

- 后端：Node.js（>=18）与 Express，前后端同一个进程
- 前端：原生 HTML、CSS 与 JavaScript，页面文件由后端静态目录直接提供，没有构建步骤
- 存储：本地 JSON 文件 `data/db.json`

## 项目结构

```
tp104/
├── server/
│   ├── index.js        # 服务入口、静态目录、接口路由
│   ├── api.js          # 规则、文件与扫描的对外动作
│   ├── rules.js        # 规则的校验、新增、修改、查询与删除
│   ├── files.js        # 文件内容的校验、收录、修改、查询与删除
│   ├── scan.js         # 按启用规则比对文件，给出命中、忽略与按目录汇总
│   ├── errors.js       # 带错误码与出错位置的业务异常
│   └── store.js        # 数据读写：载入、整理结构与落盘
├── public/
│   ├── index.html      # 主界面：规则区、文件区与命中清单区
│   ├── app.js          # 页面交互逻辑
│   └── style.css       # 页面样式
├── data/
│   └── db.json         # 本地数据文件（含十二条规则与十二个文件）
├── package.json
├── .gitignore
└── README.md
```

## 启动方式

```bash
npm install
npm start
```

启动后打开页面地址：<http://localhost:5104>

端口固定为 `5104`，如需临时改动可在启动前设置环境变量 `PORT`。

## 页面怎么用

1. 规则区列出编码、名称、级别、状态、适用文件类型与匹配写法，可以按级别、状态、适用文件类型筛选，也能按编码、名称或匹配写法搜索；支持新建、编辑与删除
2. 文件区列出收录进来的文件路径、类型、行数与更新时间，点看内容可以把整份内容展开在下面；点编辑会把路径、内容与备注回填到表单，支持收录新文件与移出清单
3. 命中清单区顶部可以选定只看某一条规则、某一个文件或者某一个级别，点按规则扫一遍，页面上给出这一轮参与比对的规则条数、范围里的文件个数、有效命中多少条、被忽略多少条
4. 按目录汇总一块把命中按文件的完整上级目录摊开：每个目录一行，写清命中多少条、涉及几个文件、提示/警告/错误各多少条，最右边一列是被忽略的条数；点目录能展开它下面的文件，点某个文件能再展开每一类规则分别命中多少条（被忽略的规则在文件里单独标出）。表格底部有合计行，目录各文件的条数相加等于目录条数，各目录相加等于这一轮总条数
5. 有效命中清单只列没有豁免的命中；带 nolint 标记的命中单独列在“被忽略的命中”一块，不混进有效计数与目录的命中列，但在忽略列与忽略表里单独计数
6. 任何一步不成立时，页面顶部给出说明，并尽量把问题标到对应的输入项上

## 比对口径

- 只有状态为启用的规则参与比对，停用的规则不会产生命中；如果指定了一条停用的规则，页面上会写明这一轮它没有参与
- 适用文件类型写成全部的规则管所有文件，写成某一种类型的只比对该类型的文件，类型按路径后缀判断（支持 js、sh、md、yml）
- 一条规则命中与否看文件里有没有出现它登记的匹配写法，区分大小写，命中的是包含这个写法的整行
- 命中按规则编码、文件路径、行号排序，同一行同时踩到多条规则时逐条各记一条

## 豁免口径（nolint）

- 某一行确实要保留命中的写法时，在文件内容里加 `nolint` 标记（不区分大小写），这一行的命中就归为“被忽略”，不计入有效命中
- js 与 sh 写在行尾即可，例如 `// nolint`、`# nolint`；md 用 `<!-- nolint -->`；yml 可以写在行尾，也可以在命中行的上一行单独写一行 `# nolint`（sh 同样认上一行）
- 被忽略的命中不进有效命中清单与目录的命中列，但每一级（总计、目录、文件）都有单独的“忽略”计数，并在页面底部单列成表；忽略条按规则的级别分布也单独给出，与有效命中的级别分布互不相加
- 标记只跟着文件内容走：把标记删掉再扫一遍，那条命中就会回到有效清单里，不需要额外维护忽略名单

## 登记口径

- 规则编码写成大写字母加短横线加数字，例如 `CODE-001`，最长 20 个字符，且不能重复
- 级别只有三种：提示、警告、错误
- 状态只有两种：启用、停用
- 匹配写法不能为空，最长 60 个字符
- 文件路径最长 120 个字符，只用字母数字、点、下划线、短横线与斜线，后缀必须是收录范围内的几种，且不能重复
- 文件内容不能为空，最长 4000 个字符

## 数据存储

所有数据写在 `data/db.json`，结构为 `{ "rules": [...], "files": [...] }`。落盘时先把内容写进同名临时文件，再用改名的方式替换正式文件，写入过程中断也不会把数据文件写坏；文件缺失或内容损坏时会自动回落到初始数据。

## 接口一览

| 方法   | 路径              | 说明                                             |
| ------ | ----------------- | ------------------------------------------------ |
| GET    | /api/health       | 服务状态检查                                     |
| GET    | /api/rules        | 规则清单，支持按级别、状态、适用文件类型筛选与关键词搜索 |
| POST   | /api/rules        | 新建规则                                         |
| GET    | /api/rules/:id    | 查看单条规则                                     |
| PATCH  | /api/rules/:id    | 修改规则                                         |
| DELETE | /api/rules/:id    | 删除规则                                         |
| GET    | /api/files        | 文件清单，支持按类型筛选与关键词搜索             |
| POST   | /api/files        | 收录文件                                         |
| GET    | /api/files/:id    | 查看单个文件（含完整内容）                       |
| PATCH  | /api/files/:id    | 修改文件                                         |
| DELETE | /api/files/:id    | 把文件移出清单                                   |
| POST   | /api/scan         | 按启用的规则扫一遍，返回有效命中、被忽略命中与按目录的两级汇总 |

接口出错时统一返回 `{ "error": { "code": "...", "message": "...", "field": "..." } }`，其中 `field` 用于让页面把问题标到具体的输入项上。
